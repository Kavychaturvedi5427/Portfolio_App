<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
<!--    now user need to have internet to use the app-->
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Portfolio_App"
        android:hardwareAccelerated="true">

        <activity
            android:name=".Contact_activity"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />
        <activity
            android:name=".Projects_activity"
            android:exported="false" />
        <activity
            android:name=".Skills_activity"
            android:exported="false" />
        <activity
            android:name=".AboutSection_activity"
            android:exported="false" /> <!-- Launcher activity -->
        <activity
            android:name=".Main_Activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Second activity -->
        <activity
            android:name=".Section_activity"
            android:exported="true" />

        <activity android:name=".Certification_activity"
            android:exported="true"
            />
    </application>


</manifest>